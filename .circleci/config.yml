
version: 2.1

jobs:
  unit-test:

    docker:
      - image: cimg/node:16.10.0
      #- image: node


    steps:
      - checkout
      - run:
          name: "npm install"
          command: "npm install"
          #command: "echo install"
      - run: npm test


workflows:
  say-hello-workflow:
    jobs:
      - unit-test
